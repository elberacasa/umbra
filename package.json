{
  "name": "umbra",
  "version": "0.1.0",
  "description": "Trust Score for AI-built software. npx umbra <path> → one score, one badge.",
  "type": "module",
  "bin": {
    "umbra": "dist/cli.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist",
    "RUBRIC.md",
    "README.md"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "test": "vitest run"
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^12.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.9"
  }
}
