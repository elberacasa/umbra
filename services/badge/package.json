{
  "name": "umbra-badge",
  "version": "0.1.0",
  "private": true,
  "description": "Umbra hosted badge service — self-reported trust scores as live badges and report pages.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "^5.5.0",
    "vitest": "^3.2.4",
    "wrangler": "^4.20.0"
  }
}
