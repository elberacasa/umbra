name = "umbra-badge"
main = "src/index.ts"
compatibility_date = "2025-06-17"

# Create the namespace once and paste the real id here:
#   npx wrangler kv namespace create SCORES
# See README.md for the full deploy runbook.
[[kv_namespaces]]
binding = "SCORES"
id = "REPLACE_WITH_KV_NAMESPACE_ID"
